@using CraftHub.Core.Contracts;
@using CraftHub.Core.Models.Course;

@model CourseServiceModel

@inject ICourseService courseService;


<div class="col-md-4">
    <div class="card mb-3">
        <div class="vesitable-img">
            <img src="https://i.pinimg.com/originals/32/9b/63/329b63886c58f6f4915b8642f52ec8b3.gif" class="img-fluid w-100 rounded-top" alt="">
        </div>
        <div class="card-body text-center">
            <h4>@Model.Title</h4>
            <h6>Description: <b>@Model.Details</b></h6>
            @if (Model.Duration == 1)
            {
                <h6>Duration: @Model.Duration <b>Month</b> </h6>
            }
            else
            {
                <h6>Duration: @Model.Duration <b>Months</b> </h6>
            }

            @if (!this.User.Identity.IsAuthenticated)
            {
                <a asp-controller="Product" asp-action="Details" asp-route-id="@Model.Id" class="btn border border-secondary rounded-pill px-3 text-primary">Details</a>
            }
           
           @* <a href="#" class="btn border border-secondary rounded-pill px-3 tex t-primary">Join Course</a>
            @if (await courseService.HasCreatorWithIdAsync(Model.Id, User.Id()) == true)
            {
                <p>LAJNA</p>
                <a asp-controller="Product" asp-action="Details" asp-route-id="@Model.Id" class="btn border border-secondary rounded-pill px-3 text-primary">Details</a>
                <a asp-controller="Product" asp-action="Edit" asp-route-id="@Model.Id" class="btn border border-secondary rounded-pill px-3 text-primary">Edit</a>
                <a asp-controller="Product" asp-action="Delete" asp-route-id="@Model.Id" class="btn border border-secondary rounded-pill px-3 text-primary">Delete</a>
            }
            else
            {
                <a asp-controller="Product" asp-action="Details" asp-route-id="@Model.Id" class="btn border border-secondary rounded-pill px-3 text-primary">Details</a>
            }        
             <p></p>
           *@

        </div>
    </div>
</div>